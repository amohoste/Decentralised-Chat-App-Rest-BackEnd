<!-- Bootstrap navbar -->
<nav class="navbar navbar-expand-sm navbar-dark bg-dark mb-3">
    <a class="navbar-brand" href="/">
        <img src="/images/logo.svg" width="30" height="30" class="d-inline-block align-top" alt="Logo" hspace="5"> DeChat</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">

            {{#if user}} <!-- check if someone is logged in -->
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="navBarDropdownMenuLink">{{user.username}}</a>
                <div class="dropdown-menu dropdown-menu-right">
                    <a href="/messages" class="dropdown-item">Messages</a>
                    <a href="/contacts" class="dropdown-item">Contacts</a>
                    <a href="/users/profile" class="dropdown-item">Profile</a>
                    <a href="/users/logout" class="dropdown-item">Logout <i class="fa fa-sign-out float-right mt-1" aria-hidden="true"></i></a>
                </div>
            </li>
            {{else}}
            <li class="nav-item">
                <a href="/users/login" class="nav-link">Login</a>
            </li>
            <li class="nav-item">
                <a href="/users/register" class="nav-link">Register</a>
            </li>
            {{/if}}
        </ul>
    </div>
</nav>